# Contributing

Contributions and suggestions are welcome.

1. Create a branch from the current `main` branch.
2. Keep changes small and easy to review.
3. Run `npm test` from the `backend` directory before opening a pull request.
4. Do not commit `.env` files, tokens, credentials, or other secrets.
